# 🌱 Vivarium

An AI agent that lives inside a Docker container, builds and maintains web apps for you via chat.

Tell Viv what you want. Viv builds it, shows you a screenshot, and keeps it running. No coding required.

## How it works

```
You:   "Build me a family chore tracker"
Viv:   ✅ Done! Here's how it looks:
       📸 [screenshot]
       🌐 http://your-server:3000

You:   "Make it darker and add a search bar"
Viv:   ✅ Updated!
       📸 [screenshot]

You:   "Save this as v1"
Viv:   💾 Saved!
```

Viv is an AI agent (Claude) running inside a Docker container with everything pre-installed — Node.js, Python, SQLite, Chromium, git. You chat via Telegram through [vivarium-hub](https://github.com/assaf-benjosef/vivarium-hub). Viv writes the code, serves the app, takes screenshots, manages versions, and fixes problems — all without you ever touching a terminal.

## Architecture

```
Telegram ─→ vivarium-hub (message broker) ─→ WebSocket ─→ Vivarium (this repo)
                                                              │
                                                              ├── Claude Agent SDK
                                                              ├── /workspace (your app)
                                                              ├── .claude/skills/ (Viv's brain)
                                                              └── .vivarium/ (notes, logs, session)
```

The vivarium connects outbound to the hub via WebSocket — no port opening needed. Your API key and data never leave your machine. The hub is a lightweight message broker that routes Telegram messages to your vivarium.

## Skills

| Skill | Purpose |
|---|---|
| `writing-notes` | Viv's memory — persists architecture, preferences, and decisions across sessions |
| `building-quality-apps` | Logging, error handling, health endpoints — so Viv can self-diagnose |
| `serving-apps` | Always serve on port 3000, auto-start on container restart |
| `managing-snapshots` | Git-based save/undo/rollback with named versions |
| `taking-screenshots` | Headless Chromium screenshots after every visual change |
| `managing-databases` | SQLite patterns and best practices |
| `environment-info` | What tools are available in the container |
| `deploying-with-ngrok` | Expose your app to the internet via ngrok |

## Quick start

### Prerequisites

- Docker
- A running [vivarium-hub](https://github.com/assaf-benjosef/vivarium-hub) instance
- An [Anthropic API key](https://console.anthropic.com/)
- A hub token (generated by the hub)

### Run

```bash
docker build -t vivarium .

docker run -d \
  -e ANTHROPIC_API_KEY=sk-ant-... \
  -e HUB_URL=wss://your-hub:8080/ws \
  -e HUB_TOKEN=eyJ... \
  -e VIVARIUM_NAME=my-app \
  -v vivarium-data:/workspace \
  -p 3000:3000 \
  --name vivarium \
  vivarium
```

Then open Telegram and message the bot connected to your hub. Viv will introduce herself.

### Environment variables

| Variable | Required | Description |
|---|---|---|
| `ANTHROPIC_API_KEY` | ✅ | Your Anthropic API key |
| `HUB_URL` | ✅ | WebSocket URL of your vivarium-hub (e.g. `wss://hub.vivarium.dev/ws`) |
| `HUB_TOKEN` | ✅ | JWT token for authenticating with the hub |
| `VIVARIUM_NAME` | | Name for this vivarium (default: `my-vivarium`) |
| `MAX_TURNS` | | Max agent turns per message (default: 30) |
| `MODEL` | | Claude model to use (default: `claude-sonnet-4-5`) |

## For non-technical users

Viv is designed for people who don't code. She:

- Never uses jargon (no "port 3000" or "npm install")
- Shows screenshots instead of describing changes
- Asks before making big changes
- Fixes problems silently when possible
- Remembers your preferences across conversations

## Development

```bash
npm install
npm run build
npm test
npm run dev   # needs env vars
```

## License

MIT
